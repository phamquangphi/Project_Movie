import {
  Pagination as PaginationA,
  PaginationProps as PaginationPropsA,
} from "antd";

export const Pagination = (props: PaginationPropsA) => {
  return <PaginationA {...props} />;
};

export default Pagination;
